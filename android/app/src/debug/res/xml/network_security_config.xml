<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network security config.
  Allows cleartext (HTTP) traffic to the local development server.

  On Android emulator, the host machine is reachable at 10.0.2.2, NOT localhost.
  Update your BASE_URL dart-define accordingly:
    --dart-define=BASE_URL=http://10.0.2.2:8080
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
